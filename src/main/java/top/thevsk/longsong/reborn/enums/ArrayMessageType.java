package top.thevsk.longsong.reborn.enums;

public enum ArrayMessageType {
    text, face, image,
    record, video, at,
    rps, dice, shake,
    poke, anonymous, share,
    contact, location, music,
    reply, forward, node,
    xml, json,
}